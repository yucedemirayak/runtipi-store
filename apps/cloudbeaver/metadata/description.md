CloudBeaver is a web-based database manager from DBeaver.

Initial setup wizard appears on first launch.

Persisted data path:
- Host: `${APP_DATA_DIR}/data`
- Container: `/opt/cloudbeaver/workspace`
